package Week2;

public class Day6 {
    /*
    Introduction to database:

    1. What is database:
        1. RDBMS: Oracle, MySQL, PostgreSQL, SQLite, Aurora(AWS)
        2. No-SQL: mongoDB, cassandra, dynamoDB
        3. cache : redis, hazelcast, (H2, derby)
        4. file  : elastic search
        5. object: S3(log / image / objects)

    2. Important Component/Concept of RDBMS:
        1. need query
        2. index(B tree, ..)
        3. transaction (ACID)
        4. lock
        5. cache
        6. disk

    3.Basic syntax of sql:
        select
        from
        where
        order by
        max
        count
        rank/dense_rank
        union/intersect/minus

        sub query


     */
}
